# GitHub Configuration
# Required: GitHub Personal Access Token with 'repo' and 'admin:repo_hook' scopes
# Get your token from: https://github.com/settings/tokens
GITHUB_TOKEN="your_github_token_here"

# Server Configuration
# Port for the local server (default: 8000)
PORT=8000

# Ngrok Configuration
# Whether to use ngrok for exposing the server (default: true)
USE_NGROK="true"

# Ngrok authentication token (recommended for stability)
# Get your token from: https://dashboard.ngrok.com/get-started/your-authtoken
NGROK_AUTH_TOKEN="your_ngrok_auth_token_here"

# Webhook URL (optional, only needed if not using ngrok or Cloudflare)
# If provided, this URL will be used for webhooks instead of ngrok or Cloudflare
# Example: WEBHOOK_URL="https://your-public-url.com/webhook"
# WEBHOOK_URL=""

# Cloudflare Configuration
# Whether to use Cloudflare Workers for exposing the server (default: false)
# If enabled, this will take precedence over ngrok
USE_CLOUDFLARE="false"

# Cloudflare API token with 'Workers Scripts:Edit' and 'Workers Routes:Edit' permissions
# Get your token from: https://dash.cloudflare.com/profile/api-tokens
CLOUDFLARE_API_TOKEN="your_cloudflare_api_token_here"

# Cloudflare account ID (found in the URL of your Cloudflare dashboard)
# Example: https://dash.cloudflare.com/abc123 (abc123 is your account ID)
CLOUDFLARE_ACCOUNT_ID="your_cloudflare_account_id_here"

# Cloudflare zone ID (optional, only needed for custom domain)
# Found in the Overview tab of your domain in Cloudflare dashboard
# CLOUDFLARE_ZONE_ID="your_cloudflare_zone_id_here"

# Cloudflare worker route pattern (optional, only needed for custom domain)
# Example: "example.com/webhook/*"
# CLOUDFLARE_WORKER_ROUTE="your_domain.com/webhook/*"